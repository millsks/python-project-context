[tool.ruff]
line-length = 100
target-version = "py311"
extend-select = ["E", "F", "I", "B", "UP", "ANN", "SIM"]
ignore = ["ANN101", "ANN102"]
src = ["src"]
exclude = [".venv", ".pixi", "build", "dist"]

[tool.ruff.per-file-ignores]
"examples/**" = ["B008"]  # FastAPI Depends() in defaults is correct pattern

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "auto"

[tool.mypy]
python_version = "3.11"
warn_unused_ignores = true
warn_redundant_casts = true
warn_return_any = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
no_implicit_optional = true
strict_optional = true
plugins = []

[tool.pytest.ini_options]
minversion = "7.0"
addopts = "-q"
testpaths = ["tests"]
